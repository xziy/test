# Repository Guidelines

- Run `npm ci` before tests to ensure dependencies are installed.
- Run `npm test` and ensure it passes before committing.
- The Docker Compose file can be used to run the server locally with `docker-compose up -d`.
- Keep changes simple and follow existing code style.
